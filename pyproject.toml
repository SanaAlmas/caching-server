[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "caching-proxy"
version = "0.1.0"

[project.scripts]
caching-proxy = "app.main:start_server"

[tool.setuptools]
packages = ["app"]